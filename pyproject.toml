[project]
name = "code-review-mcp"
version = "0.1.0"
description = "An MCP server that gives Claude tools to fetch and review GitHub PRs and code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
